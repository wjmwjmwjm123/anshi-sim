[project]
name = "anshi-sim"
version = "0.5.0"
requires-python = ">=3.13"
dependencies = [
  "fastapi>=0.123,<1",
  "pydantic>=2.11,<3",
  "uvicorn>=0.38,<1",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
cache_dir = ".pytest_tmp/cache"
